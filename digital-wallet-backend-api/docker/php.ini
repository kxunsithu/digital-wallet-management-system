; Allow profile picture uploads up to 5MB (backend validates max:5120)
upload_max_filesize = 10M
post_max_size = 12M

; Keep request execution safe for uploads
max_execution_time = 120
max_input_time = 120
memory_limit = 256M
